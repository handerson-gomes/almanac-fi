# 001 — Workspace bootstrap

## Story

As a contributor, I can install and run a strict TypeScript pnpm workspace with server, web, CLI, MCP, and package boundaries.

## Requirements

Create the documented workspace layout; configure ESM, strict TypeScript, pnpm workspace scripts, and root developer commands.

## Acceptance criteria

A clean clone supports install, typecheck, test, lint, and a development start command; every workspace package resolves through workspace dependencies.

## Dependencies

None

## Verification

Run install, typecheck, lint, test, and dev smoke checks.

## Status

Complete
