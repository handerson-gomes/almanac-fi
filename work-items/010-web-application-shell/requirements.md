# 010 — React dashboard shell

## Story

As a local user, I can open an accessible local dashboard and navigate feature routes.

## Requirements

Set up Vite, React, TanStack Router/Query, UI-kit baseline, API client, route layout, loading/error states, and local development proxy.

## Acceptance criteria

The dashboard builds and runs; navigation, not-found, API error, and keyboard-accessible shell states work; no financial feature is implied.

## Dependencies

001, 008, 009

## Verification

Build the web app and run component/router smoke tests.

## Status

Complete
